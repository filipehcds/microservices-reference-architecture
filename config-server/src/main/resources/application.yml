server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/filipehcds/microservices-reference-architecture.git
          search-paths:
          - config-repo
  security:
    user:
      name: configUser
      password: configPassword
      roles: SYSTEM